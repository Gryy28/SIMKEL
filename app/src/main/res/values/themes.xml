<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SIMKEL" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">#1024DC</item>
    </style>

    <style name="Base.Theme.SIMKEL.ActionBar" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="Theme.SIMKEL" parent="Base.Theme.SIMKEL" />
    <style name="Theme.SIMKEL.ActionBar" parent="Base.Theme.SIMKEL.ActionBar" />
</resources>